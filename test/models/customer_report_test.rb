require 'test_helper'

class CustomerReportTest < ActiveSupport::TestCase
  # test "the truth" do
  #   assert true
  # end

  test 'customer report single' do
  #Schedule.all.each{|sch| Training.create(:customer_id=>3, booking_status:sch.id.modulo(5)-1, training_status:sch.id.modulo(4), schedule_id:sch.id)}
  end
end
